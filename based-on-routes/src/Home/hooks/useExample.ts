/**
 * you can use this to create hooks, for example:
 * 
 * const useExample = () => {
 *      const [something, setSomething] = useState<any>()
 *      useEffect(() => {
 *          // do something here...
 *      }, [])
 *      return { something, setSomething }
 * }
 */